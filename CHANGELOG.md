# Changelog

## v3.1.0

### 新增：字段锁
- 任何单个字段都可以被逐一锁定，锁定状态存在画像记录的 `locked_fields` 里，锁/解锁均写审计（`lock_field` / `unlock_field`）。
- 新增配置 `field_lock_scope`，三档拦截范围：`llm_only`（默认，只拦 LLM 工具与自动提取）/ `llm_and_user`（连普通用户命令一起拦）/ `strict`（所有来源，包括管理员命令与 WebUI）。
- 新增命令 `锁定画像 <字段>`、`解锁画像 <字段>`，管理员 `锁定用户画像 <用户> <字段>`、`解锁用户画像 <用户> <字段>`。
- 面板抽屉的每个字段行都有锁按钮，锁定的行会变色、输入框禁用并显示「已锁定」标记；画像库新增「只看有锁」筛选与 `locked_desc` 排序。
- **解锁不受锁范围限制**（否则 `strict` 会把自己锁死）；`清空画像` / 删除整份画像 / `replace` 导入同样不受字段锁约束 —— 字段锁保护的是值不被悄悄改掉，不是画像删不掉。

### 新增：字段改动历史
- 新增命令 `画像历史 <字段> [条数]` 与管理员 `画像字段历史 <用户> <字段> [条数]`，直接回看某个字段的 `旧值 → 新值`、操作者、来源与原文证据。
- 面板字段行新增「历史」按钮，就地展开该字段的时间线，不用去审计页翻。
- 检索默认会一起翻已归档的 `audit_log.jsonl.<timestamp>`，轮转过也查得到；备注类条目按 `备注:<序号>` 归到 `备注` 字段下。

### 新增：完整度评分
- 完整度 = 已填基础字段 ÷ 基础字段总数 × 100%（自定义字段不计入分母，避免"越加字段越不完整"）。
- 新增命令 `画像完整度`：进度条 + 已填 / 缺失 / 已锁字段清单。
- 面板的画像卡、详情抽屉、总览页与 `画像统计` 共用同一算法；总览新增「平均完整度」「锁定字段数」「30 天活跃画像」三项指标。

### 新增：审计与备份运维
- **审计 CSV 导出**：审计页一键导出 13 列 CSV，带 UTF-8 BOM，Excel 双击即可打开，沿用当前筛选条件（`audit-csv` 接口）。
- 审计支持按 `session_id` 精确筛选，并可选把归档日志一起纳入检索。
- **备份下载**：迁移页每份备份都能原样下载成 `.json`，用于离线归档或迁移到另一台机器（`backup-download` 接口）。
- **备份删除**：面板可直接删除单份备份（二次确认），并写入审计（`delete_backup`）。
- 新增 `backup_max_count`（默认 60）：备份份数上限，与 `backup_retention_days` 两个上限同时生效。
- 新增 `audit_log_keep_rotated`（默认 5）：轮转产生的历史审计日志保留份数，超出删最旧。

### 新增字段与配置
- 基础字段从 22 项扩展到 **26 项**，新增 `代词`、`喜欢的话题`、`口头禅`、`当前目标`；`备注` 仍恒定排在最后。
- 新增 `auto_adopt_new_builtin_fields`（默认开）：插件升级带来的新内置字段会自动追加到现有字段表末尾（`备注` 之前），**只补一次**，不覆盖用户的排序与删减；配套隐式记录 `builtin_fields_adopted_version`。
- 新增命令 `画像帮助`：一次列出全部 24 条命令与当前功能开关状态。
- `_conf_schema.json` 共 31 项，`zh-CN` / `en-US` 两份 i18n 的键序与文案与 schema 完全对齐。

### 修复
- **修复身份护栏的子串误判（会静默丢数据）**：他人指代检测此前用裸子串匹配，`他` 会命中「其他 / 吉他 / 维他命」，casefold 后的 `ta` 会命中 `data` / `status` / `metadata` / `guitar`，裸 `@` 会命中邮箱地址。像「我喜欢弹吉他」这种典型自述会被判定为在谈论他人而拒绝写入。现改为带前后文断言的正则（`(?<![其吉維维利排])他`、`(?<![0-9A-Za-z])ta(?![0-9A-Za-z])`、`(?<![0-9A-Za-z._+\-])@`）。
- **修复命令护栏名单漏项**：`KNOWN_COMMAND_PREFIXES` 是硬编码的 11 条，漏掉了 v3.0.0 新增的 `画像备份` / `画像备份列表` / `画像恢复备份` / `合并画像` / `画像面板`，导致这些命令消息仍可能触发自动画像提取。现改为从 `USER_COMMANDS` / `ADMIN_COMMANDS` 指令表自动推导（`command_prefixes()`），新增命令不会再漏。
- **修复 `backup_retention_days = 0` 语义与文档不符**：构造时 `max(1, ...)` 把 0 强行抬到 1 天，配置说明写的「0 表示不清理」实际会变成「只留 1 天」，等于每天清空历史备份。现改为 `max(0, ...)`，0 真正表示不按天清理。
- **修复 WebUI 与命令侧的群聊隐私默认值不一致**：`api_meta` 里 `allow_profile_in_group` 兜底值是 `True`，而 `main.py` 与 schema 都是 `False`，未显式配置时面板会显示「群聊可查看画像」这一错误状态。
- **修复 `from __future__ import annotations` 与 `GreedyStr` 冲突**：AstrBot 的 `validate_and_convert_params` 用 `is GreedyStr` 做身份比较，延迟注解会把类型变成字符串，导致所有贪婪参数命令（`设置画像` / `删除画像` / `查询画像` …）的多词参数被截断。已移除该导入。
- 修复超大导入文件的处理顺序：先看 `Content-Length` 再决定是否读体，避免把超限文件整体读进内存后才返回 413。
- **修复超大请求体被异常处理吞掉、退化成误导性 400**：`import` 接口原本只在自己的 `try` 里判断体积，一旦 Quart 在解析阶段就因 `MAX_CONTENT_LENGTH` 抛错，异常会被外层 `except Exception` 捕获并统一回成「参数错误」400，用户完全看不出真实原因是文件太大；更糟的是 Dashboard 对未捕获异常会返回 HTML 500 页面，前端 `resp.json()` 直接解析崩溃，面板只剩一个空白报错。现在全部 19 个接口统一走 `_guard()` 兜底：请求体超限一律回 413 + 明确文案，其余未预期异常回 500 JSON，**响应永远是 JSON**，前端不会再被 HTML 错误页打崩。
- 修复面板审计页的来源筛选下拉：此前操作者类型是前端硬编码，与后端 `actor_type` 取值不同步，现由 `meta.actor_types` 下发。
- 修复删除字段 / 删除备注后写审计时直接索引 `record["subject_name"]` 的隐患（旧格式记录缺该键会抛 `KeyError`），同时去掉一次多余的重复落盘。
- 修复 `llm_tools.py` 从内部路径 `astrbot.core.agent.tool` 导入 `FunctionTool`，改用 `astrbot.api` 公开导出。
- `terminate()` 现在会显式注销三个 LLM 工具并释放 Web API 引用，反复重载插件不再残留工具注册。

### 其他
- Web 接口从 14 个增加到 **19 个**；只读模式（`webui_allow_edit=false`）下 9 个写接口统一返回 403，导出与下载不受影响。
- 备份清理逻辑重写：天数与份数双上限，自动快照 `profiles-auto-YYYYmmdd.json` 与手动/前置备份 `profiles-<标签>-YYYYmmdd-HHMMSS.json` 统一按 mtime 排序淘汰。
- 备份文件名校验改为白名单正则 `^profiles-[A-Za-z0-9_\-]+\.json$`，进一步收紧路径穿越面。
- 面板新增完整度进度条、字段锁样式与历史时间线组件，仍保持零 `innerHTML`（全部走 hyperscript）。
- README 重写：补齐 24 条命令、字段锁三档语义、完整度算法、两条明文导出通路与 Dashboard 安全须知。
- 本版随发布跑通四套回归：Quart 端到端接口测试 111 项断言、身份护栏 41 条用例、贪婪参数与指令签名 16/16/24、jsdom 前端冒烟 222 项断言（覆盖 7 套主题切换、字段锁、改动历史、导入导出、只读降级与启动失败路径）。
## v3.0.0

### 品牌
- 插件中文名由「画像织谱」更名为 **「心迹画像」**（副标题 `Profile Weaver`），同步更新 `metadata.yaml`、README 与面板标题。
- 全新设计的 `logo.png`：深绿墨色圆角卡片 + 极光光晕，主体是一颗由经纬线织成的心，交点处的光点代表被记住的画像字段。

### 新增：WebUI 管理面板
- 新增 Dashboard 插件页面 `pages/profiles`，含 5 个分区：总览、画像库、审计、迁移、关于。
- **7 套主题**：极光 / 午夜 / 樱绯 / 竹青 / 琥珀 / 石墨 / 素白，右上角实时切换并在浏览器本地记忆。
- 信息密度切换（宽松 / 紧凑）、`/` 聚焦搜索、`Esc` 关闭详情抽屉、Toast 提示、骨架屏加载、启动失败降级卡片。
- 画像库支持搜索、8 种排序、4 种筛选与分页；详情抽屉可逐字段编辑、追加/删除备注、查看每个字段的写入者与原文证据。
- 总览提供字段填充率排行、写入来源分布、最近活跃画像与审计日志体积。
- 新增 `web_api.py`：14 个 Dashboard 接口（`meta` `stats` `profiles` `profile` `profile-field` `profile-field-delete` `profile-delete` `profile-merge` `audit` `export` `import` `backups` `backup-create` `backup-restore`）。
- 新增 `webui_enabled` / `webui_allow_edit` / `webui_default_theme` 配置；只读模式下 7 个写接口统一返回 403。

### 新增：导入导出与备份
- 导出为 `profileweaver.bundle` v2 JSON，可选脱敏、可选附带最近审计记录。
- 导入支持 **merge（合并）/ overwrite（覆盖）/ replace（替换）** 三种模式，导入前自动创建 `pre-import-<mode>` 备份并写入审计。
- 每日首次写入自动快照备份，新增 `backup_retention_days` 自动清理过期备份。
- 新增命令：`画像备份 [标签]`、`画像备份列表`、`画像恢复备份 <文件名>`、`合并画像 <源key> <目标key>`、`画像面板`。
- 恢复备份前会先备份当前数据，避免误操作不可回退。

### 新增字段与配置
- 基础字段从 16 项扩展到 22 项，新增 `MBTI`、`星座`、`时区`、`常用语言`、`沟通偏好`、`禁忌话题`；`备注` 仍恒定排在最后。
- 新增 `evidence_match_mode`（`strict` / `normalized` / `loose`）：可调节 evidence 原文校验的严格度，默认 `normalized` 忽略空白与中英标点差异，减少合法写入被误拒。
- 新增 `audit_log_max_mb`：审计日志超限自动轮转为带时间戳的历史文件。
- 新增 `.astrbot-plugin/i18n/`（`zh-CN` / `en-US`）：插件名、描述、页面标题与全部 26 项配置文案支持国际化。

### 修复
- **修复脱敏导出的隐私泄漏**：开启脱敏导出时，此前只打码了 `subject_user_id` 与 `subject_name`，遗漏了 `field_meta[*].actor_id` / `actor_name`、`notes_meta` 中的操作者身份，以及 `audit[]` 里的全部身份字段。现已全部脱敏。
- 主数据文件改为**原子写**（临时文件 + 替换），避免写入中断导致 `profiles.json` 截断损坏。
- 主数据文件损坏时不再静默重置，会隔离为 `profiles.json.corrupt-<时间戳>` 后再新建，数据可人工抢救。
- 备份恢复接口拒绝路径穿越（`..` / 绝对路径），只允许恢复备份目录内的文件。
- 脱敏导出文件在导入时被明确拒绝，并给出可读原因，而不是导入出一堆打码用户。

### 其他
- 面板与命令输出统一了字段排序规则（基础字段顺序 → 自定义字段 → `备注`）。
- 前端 26 项 jsdom 冒烟测试与后端 78 项端到端接口测试全部通过。

## v2.1.5

- 修复插件运行时依赖错误声明 AstrBot 核心的问题。
- 避免 AstrBot Desktop 将第二份 AstrBot 安装到 `data/site-packages`，从而引发 `platform_stats` 数据表重复定义。
- AstrBot 版本兼容范围继续通过 `metadata.yaml` 的 `astrbot_version` 声明。

## v2.1.4

- 修复管理员指定用户画像时的 `@` 解析：`查询画像 @用户` 会使用 At 组件中的真实用户 ID，不再把展示昵称当成 ID 查询。
- 管理员的查询、修改、删除字段、清空和审计命令统一支持 QQ 号或 `@用户`。

## v2.1.3

- 调整画像展示顺序：`备注` 始终显示在最后，即使存在用户自定义字段也不会插在中间。
- 增加明确键值自述的自动写入兜底：例如 `我最喜欢的国漫：凡人修仙传` 会自动创建 `最喜欢的国漫` 字段并写入 `凡人修仙传`，无需额外消耗 LLM 工具循环 token。
- 放宽 LLM 自定义字段创建：当 `allow_llm_custom_fields` 开启时，LLM 写入未知字段会自动按当前用户自定义字段处理。
- 优化默认提示词，鼓励 LLM 在内置字段不够表达时创建贴切的当前用户自定义字段。

## v2.1.2

- 修复会话隔离模式下的画像身份冲突检测范围：同一会话内昵称、名字、备注仍会互斥查重，但不同群聊/私聊可以复用同一个称呼。
- 更新 README 对冲突检测范围的说明，避免误解为所有会话全局唯一。

## v2.1.1

- 增加画像身份冲突检测：昵称、名字、称呼、别名、网名、用户名不能和其他名字重复，也不能和备注互相复用。
- 放宽自由网名：`狐狸` 这类动物名可以作为昵称或网名，不再被硬拦截。
- 增加误导性称呼拦截：拒绝 `爸爸`、`主人`、`管理员`、`系统` 等冒犯、诱导或冒充权限的称呼写入名字或备注。
- 调整 LLM 审核策略：允许“给我的画像添加…”这类直接修改指令，只拒绝冲突、重复和明显误导/恶劣的内容。
- 修复旧数据展示顺序：上游字段 `称呼`、`名字`、`姓名`、`对用户的称呼` 自动归一到新版 `昵称`，避免被当成自定义字段排到最后。
- 同步更新 README、配置默认提示词和工具描述。

## v2.1.0

- 增加真正自动兼容旧插件目录的迁移逻辑，会探测上游常见目录并导入 `user_profiles.json`。
- 增加字段级合并和迁移签名记录，避免重复导入旧数据。
- 增加低开销自动提取流程：主对话未调用画像工具时，可额外运行短工具循环补提取画像。
- 修复 AstrBot 插件包导入问题，支持包内相对导入并补充 `__init__.py`。

## v2.0.0

- 从 [Luna-channel/astrbot_plugin_soulmap](https://github.com/Luna-channel/astrbot_plugin_soulmap) 重构 fork 为 ProfileWeaver。
- 更换插件元数据、数据目录和代码标识符，避免与上游插件冲突。
- 将隐藏标签写入模式改为 AstrBot `FunctionTool` 工具调用模式。
- 增加 LLM 查看、写入、删除画像工具。
- 增加用户命令、管理员命令、自定义字段和审计日志。
